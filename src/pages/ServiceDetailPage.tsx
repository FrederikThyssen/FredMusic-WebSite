import { PageShell } from "./PageShell";

export function ServiceDetailPage() {
  return (
    <PageShell
      eyebrow="Détail prestation"
      title="Fiche prestation"
      description="Placeholder d'une page détail prestation reliée aux données mockées."
    />
  );
}
